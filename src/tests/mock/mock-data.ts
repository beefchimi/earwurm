// The `happy-dom > fetch` will fail if passing
// a local imported asset path, so we have to
// rely on external URLs for our tests.
export const mockData = {
  audio: 'https://assets.mixkit.co/music/download/mixkit-happy-times-158.mp3',
  playDurationMs: 40,
};
